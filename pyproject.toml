[project]
name = "agrinow"
version = "0.1.0"
description = "FastAPI on Render"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "sqlalchemy",
    "psycopg2-binary",
    "cryptography",
    "PyJWT",
    "httpx",
    "jinja2",
    "python-dotenv",
]
