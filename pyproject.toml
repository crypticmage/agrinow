[project]
name = "agrinow"
version = "0.1.0"
description = "FastAPI on Cloudflare"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
]

[dependency-groups]
dev = [
    "workers-py",
]

[tool.uv]
managed = true
