[project]
name = "agrinow"
version = "0.1.0"
description = "FastAPI running on Cloudflare Workers"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "patterns-cloudflare-workers",
]

[tool.uv]
managed = true
